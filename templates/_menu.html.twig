<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            YTC <img class="devsite-product-logo" alt="MyTube" src="{{ asset('build/images/logo-youtube.svg') }}" sizes="64px" loading="lazy">
        </a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault"
                aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExampleDefault">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('video_showcase') }}">Videos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('yt_search') }}">Search</a>
                    </li>
                    {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="dropdown01" role="link"
                               data-bs-toggle="dropdown">
                                {{ 'Admin'|trans }}
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="dropdown01">
                                <li><a class="dropdown-item" href="{{ path('user_index') }}">{{ 'Users'|trans }}</a>
                                </li>
                                <li><a class="dropdown-item" href="{{ path('video_index') }}">Videos</a></li>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li><a class="dropdown-item" href="{{ path('artist_index') }}">Artists</a></li>
                                <li><a class="dropdown-item" href={{ path('genre_index') }}>Genres</a></li>
                            </ul>
                        </li>
                    {% endif %}
                    {% endif %}
                </ul>
            <div class="ms-auto">
                {% if app.user %}
                    <a class="nav-link" accesskey="l" href="{{ path('logout') }}" title="{{ 'Logout'|trans }}">
                        <span class="oi oi-account-logout"></span>
                    </a>
                {% else %}
                    <a class="nav-link" accesskey="l" href="{{ path('login') }}" title="{{ 'Login'|trans }}">
                        <span class="oi oi-account-login"></span>
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
